# This is intentionally NOT a complete public API configuration.
# Do not expose /var/lib/moodify or node.sqlite3.
# If the existing Moodify FastAPI service is published through nginx,
# proxy only explicit application endpoints and keep job submission authenticated.
#
# Example skeleton after Codex verifies the actual API port:
#
# location /api/ {
#     proxy_pass http://127.0.0.1:8000/;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     client_max_body_size 200M;
# }
