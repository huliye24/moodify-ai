# HANDOFF CONTRACT — PACKAGE 013

012 should leave 013 a stable foundation with contracts such as:

```text
WalletSession
ResidentIdentitySeed
ContributionTask
ContributionSubmission
ReputationSnapshot / ReputationEvents
TransparencySnapshot
LaunchState
```

013 may build:
- WORLD route shell
- PROTOCOL route shell
- PORTAL route shell
- navigation
- visitor vs connected-wallet states
- visual spatial hierarchy

013 must NOT:
- invent official Token CA
- enable DEX
- enable claim
- expose Genesis distribution as active
- create token-gated identity
- rewrite contribution domain rules

If 012 cannot provide stable data contracts, mark 013 BLOCKED rather than letting UI invent them.
