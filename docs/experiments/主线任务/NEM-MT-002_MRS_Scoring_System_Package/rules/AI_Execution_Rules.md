# AI Execution Rules｜AI 执行规则

1. 先读 `00_PACKAGE_MANIFEST.json`。
2. 再读 `00_NODE_STATUS.md`。
3. 再读 `rules/MRS_Benchmark_Rules.md`。
4. 不要跳过 Gate。
5. 不要一次推进多个 AEP。
6. 不要把 MRS 改成满分制。
7. 不要让 MRS 阻塞 Runtime 主流程。
8. 不要删除历史报告。
9. 每次公式或权重变化必须写入 `decisions/Decision_Log.md`。
10. 验证失败时使用 HOLD，不要伪装成 PASS。
