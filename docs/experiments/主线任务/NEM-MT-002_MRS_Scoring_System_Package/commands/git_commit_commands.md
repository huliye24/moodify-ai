# Git Commit Commands

## 建议提交信息

```bash
git add .
git commit -m "Add MT-002 MRS scoring benchmark node package"
git push
```

## 如果只提交 MRS 节点文档

```bash
git add docs/nem/MT-002 docs/aep/MT-002 docs/gate/MT-002
git commit -m "Document MRS benchmark node structure"
```
