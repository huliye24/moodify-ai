# MHP-010 回传单：ChatGPT — Inspector 响度匹配增强

## 核心判断

三组处理后 RMS 明显高于 before（+5~9 dB），存在 loudness bias。
Inspector 必须先支持响度匹配，再进行正式听感验收。

## MHP-009-A1 任务

增强 inspector：RMS delta 计算、loudness warning、--write-matched-after、精度 2 位小数
