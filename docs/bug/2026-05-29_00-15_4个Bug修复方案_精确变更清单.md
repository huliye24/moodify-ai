# 4 个 Bug 修复方案 · 精确变更清单

**时间**: 2026-05-29 00:15
**前提**: 已确认 4 个 Bug（详见 `2026-05-28_23-59_静态推演发现的4个Bug与3个设计缺陷.md`）
**策略**: 最小变更原则——每个 Bug 只改必须改的签名和逻辑，不趁机重构。

---

## 执行顺序（重要：必须按此顺序，否则会产生冲突）

```
Bug 4 (defects 复用) → Bug 2 (vector_bias 贯通) → Bug 1 (版本后缀) → Bug 3 (RAG 校验)

理由:
  - Bug 4 在 search_optimal_strengths 签名的末尾加参数
  - Bug 2 在同一个函数的末尾再加参数
  - 顺序反过来会导致第二次修改时冲突
  - Bug 1 和 3 独立，放最后
```

---

## Bug 4：DefectClassifier 三重调用 → 一重

### 变更范围

| 文件 | 函数 | 变更类型 |
|------|------|---------|
| `optimizer/search.py` | `search_optimal_strengths()` | 签名 + 参数 |
| `optimizer/search.py` | `define_strength_space()` | 不变（仍接收 defects） |
| `orchestration/workflow_engine.py` | `_run_strength_search()` | 签名 + 传参 |
| `orchestration/workflow_engine.py` | `_run_diagnosis()` | 不变（已经产出 defects） |
| `orchestration/workflow_engine.py` | `process()` — RAG 路径 | 复用 phase1 的 defects |

### 变更 1：`search_optimal_strengths()` 签名

```python
# 旧
def search_optimal_strengths(
    diagnosis,
    emotion_target: str,
    top_k: int = 3,
    n_samples: int = 2000,
) -> list[tuple[dict[str, float], dict[str, float], float]]:

# 新 — 只在末尾加一个可选参数
def search_optimal_strengths(
    diagnosis,
    emotion_target: str,
    top_k: int = 3,
    n_samples: int = 2000,
    defects: list | None = None,          # ← 新增。为 None 时内部自分类（兼容旧调用方）
) -> list[tuple[dict[str, float], dict[str, float], float]]:
```

内部逻辑改动（`optimizer/search.py:211-212`）:

```python
# 旧
classifier = DefectClassifier()
defects = classifier.classify(diagnosis, emotion_code)

# 新
if defects is None:
    classifier = DefectClassifier()
    defects = classifier.classify(diagnosis, emotion_code)
```

### 变更 2：`_run_strength_search()` 签名 — 透传 defects

```python
# 旧
def _run_strength_search(self, ws_diagnosis, emotion_target: str) -> PhaseResult:

# 新
def _run_strength_search(self, ws_diagnosis, emotion_target: str,
                         defects=None) -> PhaseResult:
```

内部调用改动:

```python
# 旧
results = search_optimal_strengths(ws_diagnosis, emotion_target, top_k=3)

# 新
results = search_optimal_strengths(ws_diagnosis, emotion_target, top_k=3, defects=defects)
```

### 变更 3：`process()` — Phase 1.5 传入 defects

```python
# 旧 (workflow_engine.py:189-192)
phase1_5 = self._run_strength_search(
    phase1.output.get("wave_state_diagnosis"),
    emotion_target,
)

# 新
phase1_5 = self._run_strength_search(
    phase1.output.get("wave_state_diagnosis"),
    emotion_target,
    defects=phase1.output.get("defects"),    # ← Phase 1 已分类, 复用
)
```

### 变更 4：`process()` — RAG 路径复用 defects（删掉重复分类）

```python
# 旧 (workflow_engine.py:147-153)
from moodify.diagnosis.defect_classifier import DefectClassifier
classifier = DefectClassifier()
defects = classifier.classify(
    phase1.output.get("wave_state_diagnosis"), resolved_emotion_key
)
defects_list = [{"parameter": d.parameter, "severity": d.severity} for d in defects]

# 新 — 直接读 Phase 1 的产出
defects = phase1.output.get("defects", [])
defects_list = [{"parameter": d.parameter, "severity": d.severity} for d in defects]
# DefectClassifier 的 import 一并删除
```

### 验证

```python
# 确认 defects 从 Phase 1 正确传递到搜索
# 确认不传 defects 时函数行为不变（向后兼容）
from moodify.optimizer.search import search_optimal_strengths
results = search_optimal_strengths(ws, "GA", top_k=3)          # 无 defects → 内部分类
results2 = search_optimal_strengths(ws, "GA", top_k=3, defects=some_defects)  # 有 defects → 跳过
assert len(results) == len(results2) == 3
```

---

## Bug 2：vector_bias 全程贯通

### 策略选择

有两种方案：

**方案 A（EmotionContext 值对象）**: 新建 dataclass，替换所有 `emotion_code: str` 参数。干净但改动面大（~8 个函数签名）。

**方案 B（可选参数透传）**: 在现有签名末尾加 `vector_bias: dict | None = None`，接收方内部应用 bias。改动面小（~5 个函数），向后兼容。

**选择方案 B**。理由：Bug 修复不应与架构重构耦合。方案 A 属于"缺陷 5/6/7 重构"的范畴。

### 变更范围

| 文件 | 函数 | 变更 |
|------|------|------|
| `optimizer/search.py` | `proxy_evaluate()` | + `vector_bias` 参数，应用到 target |
| `optimizer/search.py` | `search_optimal_strengths()` | + `vector_bias` 参数，透传给 proxy_evaluate |
| `orchestration/workflow_engine.py` | `_run_strength_search()` | + `vector_bias` 参数，透传给 search |
| `orchestration/workflow_engine.py` | `_compute_eds()` | + `vector_bias` 参数，透传给 scorer |
| `diagnosis/health_scorer.py` | `compute_eds()` | + `target_vec` 可选参数 |
| `orchestration/workflow_engine.py` | `process()` | 从 emotion_parsed 取 bias，传给上下游 |

### 变更 1：`proxy_evaluate()` — bias 应用于目标向量

```python
# 旧签名
def proxy_evaluate(
    strength_vector: dict[str, float],
    diagnosis,
    emotion_code: str,
    craft_card=None,
) -> float:

# 新签名
def proxy_evaluate(
    strength_vector: dict[str, float],
    diagnosis,
    emotion_code: str,
    craft_card=None,
    vector_bias: dict | None = None,    # ← 新增
) -> float:
```

内部改动（`optimizer/search.py:122-124`）:

```python
# 旧
target = get_ideal_process_vector(emotion_code)
dist_before = float(np.linalg.norm(ws_raw.to_array() - target))
dist_after  = float(np.linalg.norm(ws_proxy.to_array() - target))

# 新
target = get_ideal_process_vector(emotion_code).copy()
if vector_bias:
    for i, dim in enumerate(["E", "D", "S", "T", "H"]):
        target[i] += vector_bias.get(dim, 0.0)
    target = np.clip(target, 0.0, 1.0)
dist_before = float(np.linalg.norm(ws_raw.to_array() - target))
dist_after  = float(np.linalg.norm(ws_proxy.to_array() - target))
```

注意: 要用 `.copy()` 因为 `get_ideal_process_vector` 返回的可能是缓存的同一个数组对象。不 copy 会污染原数组。

### 变更 2：`search_optimal_strengths()` — 透传 bias

```python
# 旧签名（Bug 4 已改后）
def search_optimal_strengths(
    diagnosis,
    emotion_target: str,
    top_k: int = 3,
    n_samples: int = 2000,
    defects: list | None = None,
) -> list[tuple[dict[str, float], dict[str, float], float]]:

# 新签名
def search_optimal_strengths(
    diagnosis,
    emotion_target: str,
    top_k: int = 3,
    n_samples: int = 2000,
    defects: list | None = None,
    vector_bias: dict | None = None,      # ← 新增
) -> list[tuple[dict[str, float], dict[str, float], float]]:
```

内部（`optimizer/search.py:234`）:

```python
# 旧
score = proxy_evaluate(vec, diagnosis, emotion_code, craft_card)

# 新
score = proxy_evaluate(vec, diagnosis, emotion_code, craft_card, vector_bias=vector_bias)
```

### 变更 3：`_run_strength_search()` — 透传 bias

```python
# 旧签名（Bug 4 已改后）
def _run_strength_search(self, ws_diagnosis, emotion_target: str,
                         defects=None) -> PhaseResult:

# 新签名
def _run_strength_search(self, ws_diagnosis, emotion_target: str,
                         defects=None, vector_bias=None) -> PhaseResult:
```

内部:

```python
# 旧
results = search_optimal_strengths(ws_diagnosis, emotion_target, top_k=3, defects=defects)

# 新
results = search_optimal_strengths(ws_diagnosis, emotion_target, top_k=3,
                                   defects=defects, vector_bias=vector_bias)
```

### 变更 4：`HealthScorer.compute_eds()` — 接受预计算的目标向量

```python
# 旧
def compute_eds(self,
                ws_raw: WaveStateDiagnosis,
                ws_processed: WaveStateDiagnosis,
                emotion_target: str) -> float:

# 新
def compute_eds(self,
                ws_raw: WaveStateDiagnosis,
                ws_processed: WaveStateDiagnosis,
                emotion_target: str,
                target_vec: np.ndarray | None = None) -> float:   # ← 新增
```

内部（`diagnosis/health_scorer.py:119`）:

```python
# 旧
target_vec = self._get_ideal_vector(emotion_target)

# 新
if target_vec is None:
    target_vec = self._get_ideal_vector(emotion_target)
# 其余逻辑不变
```

### 变更 5：`_compute_eds()` — 计算 biased vector 并传入

```python
# 旧
def _compute_eds(self, ws_before, ws_after, emotion: str) -> float:
    if ws_before is None or ws_after is None:
        return 0
    from moodify.diagnosis.health_scorer import HealthScorer
    scorer = HealthScorer()
    return scorer.compute_eds(ws_before, ws_after, emotion)

# 新
def _compute_eds(self, ws_before, ws_after, emotion: str,
                 vector_bias: dict | None = None) -> float:
    if ws_before is None or ws_after is None:
        return 0
    from moodify.diagnosis.health_scorer import HealthScorer
    scorer = HealthScorer()
    target = None
    if vector_bias:
        target = scorer._get_ideal_vector(emotion).copy()
        for i, dim in enumerate(["E", "D", "S", "T", "H"]):
            target[i] += vector_bias.get(dim, 0.0)
        target = np.clip(target, 0.0, 1.0)
    return scorer.compute_eds(ws_before, ws_after, emotion, target_vec=target)
```

注意: 这里调了 `_get_ideal_vector` 这个"私有"方法。这是最小变更策略下可接受的权衡——在重构时可以把 bias 计算抽成 `emotion_targets.py` 的公开函数。

### 变更 6：`process()` — 两处调用传入 bias

**位置 A** — Phase 1.5:

```python
# 旧
phase1_5 = self._run_strength_search(
    phase1.output.get("wave_state_diagnosis"),
    emotion_target,
    defects=phase1.output.get("defects"),
)

# 新
phase1_5 = self._run_strength_search(
    phase1.output.get("wave_state_diagnosis"),
    emotion_target,
    defects=phase1.output.get("defects"),
    vector_bias=emotion_parsed.get("vector_bias"),    # ← 传入
)
```

**位置 B** — Phase 4-6 循环内的 EDS 测量:

```python
# 旧 (workflow_engine.py:244)
eds_a = self._compute_eds(ws_diagnosis, ws_a, emotion_target)

# 新
eds_a = self._compute_eds(ws_diagnosis, ws_a, emotion_target,
                          vector_bias=emotion_parsed.get("vector_bias"))
```

### 验证

```python
import numpy as np
from moodify.knowledge.emotion_targets import get_ideal_process_vector

# 验证 bias 效果
stock = get_ideal_process_vector("GA")  # GA 的 stock 理想向量
bias = {"E": 0.10, "D": 0.0, "S": 0.0, "T": 0.0, "H": 0.0}

# 模拟 proxy_evaluate 的 bias 应用
biased = stock.copy()
biased[0] += 0.10
assert biased[0] == stock[0] + 0.10
assert biased[1] == stock[1]  # 其他维不变

# 验证 clip
bias_extreme = {"E": 1.0, "D": 0.0, "S": 0.0, "T": 0.0, "H": 0.0}
biased2 = stock.copy()
biased2[0] += 1.0
biased2 = np.clip(biased2, 0.0, 1.0)
assert biased2[0] <= 1.0
```

---

## Bug 1：_run_mastering 文件名冲突

### 变更范围

| 文件 | 函数 | 变更 |
|------|------|------|
| `orchestration/workflow_engine.py` | `_run_mastering()` | + `version_suffix` 参数 |
| `orchestration/workflow_engine.py` | `process()` — 循环内调用 | 传入版本号 |

### 变更 1：`_run_mastering()` 签名

```python
# 旧
def _run_mastering(self, audio: np.ndarray, sr: int,
                    input_path: str, emotion_target: str,
                    platform: str = "spotify") -> PhaseResult:

# 新
def _run_mastering(self, audio: np.ndarray, sr: int,
                    input_path: str, emotion_target: str,
                    platform: str = "spotify",
                    version_suffix: str = "") -> PhaseResult:    # ← 新增
```

内部两处导出点都加后缀:

**主路径**（line 615）:
```python
# 旧
output_filename = f"{base_name}_{emotion_short}_{platform}.wav"

# 新
output_filename = f"{base_name}_{emotion_short}_{platform}{version_suffix}.wav"
# 例: "test_GA_spotify_v0.wav", "test_GA_spotify_v1.wav", "test_GA_spotify_v2.wav"
```

**fallback 路径**（line 649）:
```python
# 旧
f"{base_name}_{emotion_short}_{platform}.wav"

# 新
f"{base_name}_{emotion_short}_{platform}{version_suffix}.wav"
```

### 变更 2：`process()` 循环内传入版本号

```python
# 旧 (workflow_engine.py:237)
p6 = self._run_mastering(p5.output["audio"], sr, input_path, emotion_target, platform)

# 新
p6 = self._run_mastering(p5.output["audio"], sr, input_path, emotion_target, platform,
                         version_suffix=f"_v{i}")
```

### 副作用检查

`_run_mastering` 的其他调用方：

搜索整个代码库，`_run_mastering` 只在两个地方被调用：
1. `process()` 的 Phase 4-6 循环（line 237）— 已修改
2. 无其他调用方

因为 `version_suffix` 有默认值 `""`，任何未来调用方不受影响。

### 验证

```python
# 模拟多版本输出
# Version 0 → test_GA_spotify_v0.wav
# Version 1 → test_GA_spotify_v1.wav  
# Version 2 → test_GA_spotify_v2.wav
# 确认三个文件均存在且内容不同
```

---

## Bug 3：RAG 参数无边界校验

### 变更范围

单文件单位置：`orchestration/workflow_engine.py` Phase 1.3 RAG 路径。

### 变更

在 RAG 参数赋值之后，立即插入校验逻辑：

```python
# workflow_engine.py:164-168 之后插入

# 旧
result = llm.recommend_params(prompt)
if result and "parameters" in result and "strength_vector" in result:
    rag_params = {p["param_name"]: p["value"] for p in result["parameters"]}
    rag_strength = result["strength_vector"]
    rag_confidence = result.get("confidence", 0.7)

# 新
result = llm.recommend_params(prompt)
if result and "parameters" in result and "strength_vector" in result:
    rag_params = {p["param_name"]: p["value"] for p in result["parameters"]}
    rag_strength = result["strength_vector"]
    rag_confidence = result.get("confidence", 0.7)
    
    # ── 边界校验 ──
    from moodify.knowledge.craft_chains import CRAFT_CHAINS_15PARAMS, PARAM_KEYS, get_recommended_params
    emotion_code = emotion_parsed.get("emotion_code", "GA")
    chain = CRAFT_CHAINS_15PARAMS.get(emotion_code, {})
    rec_params = get_recommended_params(emotion_code)
    
    # 1. 补齐缺失参数
    for pk in PARAM_KEYS:
        if pk not in rag_params:
            rag_params[pk] = rec_params.get(pk, 0.0)
    
    # 2. clamp 每个参数到 [min, max]
    for pk in PARAM_KEYS:
        spec = chain.get(pk)
        if spec and pk in rag_params:
            rag_params[pk] = max(spec["min"], min(spec["max"], rag_params[pk]))
    
    # 3. clamp 强度向量
    for dim in ["spectrum", "dynamic", "space", "layer", "master"]:
        if dim in rag_strength:
            rag_strength[dim] = max(0.05, min(0.95, rag_strength[dim]))
```

### 设计要点

- 补齐缺失参数（LLM 可能遗漏某个键）——确保始终有 15 个参数
- clamp 到 min/max——确保不越界
- 强度向量也 clamp——`llm.recommend_params` 的 prompt 约束了 [0.15, 0.85]，但 LLM 不总是遵守

### 验证

```python
# 模拟 LLM 输出越界参数
fake_params = {"P06_compression_ratio": 20.0, "P10_reverb_t60": -0.5}
# → clamp 后: P06 = 2.5 (GA max), P10 = 0.8 (GA min)

# 模拟 LLM 输出缺参数
fake_params = {"P02_vocal_presence_gain": 2.5}  # 只有 1 个
# → 补齐后: 15 个参数
```

---

## 变更总览

```
本次修复涉及:
  optimizer/search.py                2 个函数签名变更 (search_optimal_strengths, proxy_evaluate)
  diagnosis/health_scorer.py         1 个函数签名变更 (compute_eds)
  orchestration/workflow_engine.py   4 个函数签名变更 (_run_mastering, _run_strength_search, _compute_eds)
                                     2 个 process() 内联逻辑变更 (RAG 校验, defects 复用)

签名变更全部采用 "末尾加可选参数 + 默认值" 模式 → 100% 向后兼容。
```

### 不需要改的文件

```
llm/client.py           ← 不变。DeepSeekClient 的三个方法接口无变化。
llm/prompt_assembler.py ← 不变。RAG prompt 组装逻辑无变化。
llm/offline_fallback.py ← 不变。离线聚合逻辑无变化。
memory/history.py       ← 不变。ProcessingHistory 接口无变化。
optimizer/search.py     ← 核心算法（LHS/代理评估/strength_to_params）不变，只在接口层加参数。
knowledge/*             ← 不变。craft_chains 和 emotion_targets 数据无变化。
```

---

## 执行清单（9 步）

```
Bug 4（defects 复用）
├── 1. optimizer/search.py: search_optimal_strengths() 签名 + defects 参数
├── 2. workflow_engine.py: _run_strength_search() 签名 + defects 透传
├── 3. workflow_engine.py: process() — Phase 1.5 传入 defects, RAG 路径删重复分类

Bug 2（vector_bias 贯通）
├── 4. optimizer/search.py: proxy_evaluate() 签名 + vector_bias 应用
├── 5. optimizer/search.py: search_optimal_strengths() 透传 vector_bias
├── 6. diagnosis/health_scorer.py: compute_eds() 签名 + target_vec 可选参数
├── 7. workflow_engine.py: _run_strength_search() + _compute_eds() 透传 vector_bias
├── 8. workflow_engine.py: process() — Phase 1.5 + EDS 测量处 传入 vector_bias

Bug 1（文件名冲突）
├── 9. workflow_engine.py: _run_mastering() + process() 调用处 加版本后缀

Bug 3（RAG 校验）
└── 10. workflow_engine.py: RAG 路径加 clamp + 补齐逻辑

每步完成后跑: python -c "from moodify.orchestration.workflow_engine import WorkflowOrchestrator"
确保无 import 错误。
```
