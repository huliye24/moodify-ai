# 2026-08-04｜数据资产任务编排

## 当日既有任务

周计划中的 `WSE-02` 保持不变：对一首有权样本运行可用测量，生成单案例 WSE MeasurementRecord。它为数据治理提供真实但不自动可训练的研究证据。

## 新增串行任务

`DSK-MFY-DATA-ASSET-013｜数据资产治理与晋升基础 v0.1`

```text
E:\moodify\docs\tasks\deepseek\DSK-MFY-DATA-ASSET-013\
```

前置依赖：`DSK-MFY-SPECTRAL-EVIDENCE-012` 已形成可审计 HANDOFF。013 必须在 WSE-02 及共享目录写入结束后串行执行，不得与其他 Worker 同时修改相同目录。

## 建议新增时间盒（5 小时）

1. 60 分钟：冻结身份、权利用途、隐私、lineage、撤回和晋升合同；
2. 90 分钟：建立只读资产 catalog、质量画像和 quarantine ledger；
3. 90 分钟：建立晋升 proposal、数据集快照和多维 scorecard；
4. 60 分钟：撤回传播、泄漏防护、失败注入和 App 数据飞轮需求。

这是周计划之外的独立新增窗口。如果没有完整的额外 5 小时，可在阶段边界停止并顺延实际实施，但保留 2026-08-04 的任务登记，不并行赶工、不降低门禁、不虚报完成。

## 完成门槛

- 运营、研究、候选和获批训练数据四层隔离；
- 训练权必须显式存在，未知或待定权利 fail-closed；
- 目录与数据集快照不复制受限媒体正文；
- 晋升仅形成 proposal，未经人工批准不得进入训练层；
- 删除/撤回可传播到派生资产和快照；
- 数据价值分维度报告，不用文件总数或单分数冒充价值；
- 不训练模型、不开发 App、不接入生产。

