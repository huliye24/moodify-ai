# MHP-884: Judge Command Checker
**Status**: done
**Direction**: ECHAIN-MOODIFY-MAP-CHAIN-015 / System 6A / V4

## Implementation
`map_judge_check.py runtime` runs proof commands. `map_judge_check.py arch` evaluates diff risk (low/medium/high). Integrated into the `all` subcommand.
