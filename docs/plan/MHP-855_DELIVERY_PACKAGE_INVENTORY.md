# MHP-855: Delivery Package Inventory

**Status**: done
**Direction**: ECHAIN-MOODIFY-MAP-CHAIN-015 / NEM-MOODIFY-MAP-PROBE-045 / Probe Plan-6B: Vector Definitions / S5
**Depends on**: MHP-845 (Audit), MHP-846 (Interface Contract)
**Protocol**: AWJ Stack + E-Chain 54

## Context

The v01 pipeline produces a `DeliveryBundle` with 5 paths. MAP requires a reproducible package with metadata, logs, manifest, and environment. This probe inventories what exists and what's missing.

## Current Delivery (v0.1)

```text
output_dir/
  vocal_folk_clean_master.wav               # processed audio
  vocal_folk_clean_master_report.json       # delivery report
  vocal_folk_clean_master_report.pdf        # PDF report
  vocal_folk_before_spectrum.png            # before spectrum chart
  vocal_folk_clean_master_after_spectrum.png # after spectrum chart
```

| Artifact | Extension | Required | Generated by |
|----------|-----------|----------|-------------|
| Processed audio | `.wav` | yes | `v01_exporter.export()` |
| JSON report | `_report.json` | yes | `v01_pipeline._save_report()` |
| PDF report | `_report.pdf` | yes | `v01_pipeline._save_pdf_report()` |
| Before spectrum | `_before_spectrum.png` | yes | `v01_analyzer._save_spectrum_png()` |
| After spectrum | `_after_spectrum.png` | yes | `v01_analyzer._save_spectrum_png()` |

Total: 5 files. All exist. 0 files are schema-validated against MAP contract (report JSON was validated in MHP-848).

## MAP Target Delivery (v0.2)

Seven new artifacts required:

| # | Artifact | Spec | Owner | Build MHP |
|---|----------|------|-------|-----------|
| 1 | `manifest.json` | Paths, sizes, hashes of all artifacts | Worker | MHP-875 |
| 2 | `metadata.json` | git_hash, python_version, package_versions, platform, timestamp, hostname | Worker | MHP-876 |
| 3 | `processing.log` | Console output capture | Worker | MHP-875 |
| 4 | `environment.txt` | pip freeze or `sys.version` + dependency versions | Worker | MHP-876 |
| 5 | `delivery_manifest.csv` | Machine-readable CSV version of manifest | Worker | MHP-875 |
| 6 | `validation_report.json` | Just the QualityGate/ValidationResult, standalone | Worker | MHP-877 |
| 7 | `MAP_CHAIN_VERSION` | Text file with schema version: `map_chain_v0.2` | Worker | MHP-875 |

Full delivery directory:

```text
output_dir/
  # -- audio (existing) --
  vocal_folk_clean_master.wav
  # -- reports (existing) --
  vocal_folk_clean_master_report.json       # full report (MAP schema validated)
  vocal_folk_clean_master_report.pdf
  # -- charts (existing) --
  vocal_folk_before_spectrum.png
  vocal_folk_clean_master_after_spectrum.png
  # -- MAP v0.2 delivery (new) --
  manifest.json                             # artifact inventory
  metadata.json                             # reproducibility metadata
  environment.txt                           # dependency versions
  processing.log                            # captured output
  delivery_manifest.csv                     # CSV view of manifest
  validation_report.json                    # standalone QualityGate
  MAP_CHAIN_VERSION                         # "map_chain_v0.2"
```

Total: 12 files (5 existing + 7 new).

## Manifest Schema

```json
{
  "map_chain_version": "0.2.0",
  "run_id": "20260605_vocal_folk_clean_master",
  "generated_at": "2026-06-05T12:00:00Z",
  "artifacts": [
    {
      "path": "vocal_folk_clean_master.wav",
      "role": "output_audio",
      "size_bytes": 8640172,
      "sha256": "abc123...",
      "format": "wav",
      "sample_rate": 48000,
      "channels": 2,
      "duration_s": 45.0
    }
  ],
  "pipeline": {
    "version": "0.1.0",
    "stages": ["S_scan","A_analyze","D_diagnose","P_process","V_validate","R_report","G_generate"],
    "preset": "clean_master",
    "elapsed_s": 0.8
  }
}
```

## Reproducibility Metadata

```json
{
  "git_hash": "abc1234",
  "git_branch": "main",
  "python_version": "3.12.3",
  "platform": "Linux-6.8.0-117-generic-x86_64",
  "hostname": "moodify-worker-01",
  "packages": {
    "numpy": "1.26.4",
    "scipy": "1.13.0",
    "pedalboard": "0.9.14",
    "matplotlib": "3.8.4"
  },
  "input_sha256": "def5678..."
}
```

## Acceptance Criteria

- [x] Current 5-file delivery inventory documented.
- [x] Target 12-file delivery defined with 7 new artifacts.
- [x] Manifest schema specified.
- [x] Reproducibility metadata schema specified.
- [x] Each new artifact has a Build NEM MHP owner.
