# 接力文档：SPEC-002 → SPEC-002-REV 迁移指南

**目标读者**: AI 编码智能体
**前置条件**: SPEC-002（2026-05-28 19:45）已完整实现
**目标状态**: SPEC-002-REV（2026-05-28 23:00）
**策略**: 在现有实现基础上修改，尽量保留可复用的代码

---

## 0. 差距总览

```
SPEC-002 产物                  →  SPEC-002-REV 处理方式
═══════════════════════════════════════════════════════════
optimizer/params_to_strengths.py  →  ✗ 删除（正向映射 → 反向映射, 移入 search.py）
optimizer/proxy_metric.py         →  ✗ 删除（独立函数 → 移入 search.py 的 proxy_evaluate）
optimizer/search.py               →  ↻ 重写（15D网格 → 5D LHS, 逻辑完全不同）
llm/schemas.py                    →  ✗ 删除（模型合并到 client.py, 内容完全不同）
llm/prompts.py                    →  ✗ 删除（Prompt 合并到 client.py, 角色完全不同）
llm/client.py                     →  ↻ 重写（define_search_space → interpret_emotion）
memory/history.py                 →  ✓ 保留不动（推迟到阶段 C, 现有代码作为资产保留）
workflow_engine.py                →  △ 修改（Phase 1.5 内部逻辑 + 新增 Phase 0）
craft_chain_match.py              →  ↩ 回退（删除 DEPRECATED 标记, 恢复引用）
emotion_targets.py                →  ＋ 追加（新增 resolve_emotion_from_nl 函数）
```

**工作量对比**:
- SPEC-002: 约 690 行新代码 + 130 行改动 = 820 行
- 本次接力: 约 320 行新代码 + 100 行改动 + 120 行删除 = 实际净增量约 300 行
- 接力大约是原始工作量的 40%

---

## 1. 保留不动（无需任何操作）

这些文件/代码是 SPEC-002 的正确产出，REV 不改变它们：

```
optimizer/__init__.py        ← 空文件, 不变
llm/__init__.py               ← 空文件, 不变  
memory/__init__.py            ← 空文件, 不变
memory/history.py             ← 完整保留, 阶段 C 启用
```

`memory/history.py` 的 `ProcessingHistory`、`ProcessingRecord`、`diagnosis_to_vector` 三个组件在 REV 中完全不变。它们当前未被 workflow_engine 调用（因为阶段 C 推迟了），但代码是正确的资产，不需要删除。

---

## 2. 删除文件（4 个文件，直接 rm）

```bash
# 在 moodify-core-package/src/moodify/ 下执行
rm optimizer/params_to_strengths.py   # 15→5 正向映射, REV 用反向映射替代
rm optimizer/proxy_metric.py          # 独立代理函数, REV 整合进 search.py
rm llm/schemas.py                     # 搜索空间相关模型, REV 改为情绪解析模型
rm llm/prompts.py                     # 搜索空间设计师 prompt, REV 改为情绪解释器 prompt
```

---

## 3. 重写文件（2 个文件，完全替换内容）

### 3.1 `optimizer/search.py` — 完全重写

**为什么必须重写而非修改**: 原版的核心循环是 "缺陷→活跃15D参数→meshgrid→评估15D候选"。修订版是 "缺陷→5D强度范围→LHS采样→评估5D强度→反推参数"。数据流方向不同，无法通过局部修改完成。

**删除原文件全部内容，写入以下新内容**:

> 从 `2026-05-28_23-00_SPEC-002-REV_DeepSeek集成_AI可执行规格书_修订版.md` 的 Section 2.1.1 → 2.1.6 逐节复制实现。
> 即：常量定义 → define_strength_space() → sample_strength_vectors() → proxy_evaluate() → strength_to_params() → search_optimal_strengths()

**特别注意**:
- 原 `search_optimal_params()` 改名为 `search_optimal_strengths()`。调用方（workflow_engine.py）必须同步改名。
- 原 `_fill_frozen_params()` 删除。REV 不需要它——`strength_to_params()` 直接输出完整 15 参数。
- 新增 `from scipy.stats import qmc` 依赖（LHS 采样）。

---

### 3.2 `llm/client.py` — 完全重写

**为什么必须重写而非修改**: 原版 LLM 方法 `define_search_space()` 的输入是 diagnosis+defects+emotion，输出是 active_dimensions+frozen。修订版的核心方法是 `interpret_emotion()`——输入是自然语言文本，输出是 emotion_code+intensity+vector_bias。输入输出类型完全不同，Pydantic 模型也不同。

**删除原文件全部内容，写入以下新内容**:

> 从 `2026-05-28_23-00_SPEC-002-REV_DeepSeek集成_AI可执行规格书_修订版.md` 的 Section 3.1.1 → 3.1.3 逐节复制实现。
> 即：Pydantic 模型（EmotionInterpretation, DiagnosisNarrative）→ Prompt 常量 → DeepSeekClient 类

**可复用的部分**（从原 client.py 拷贝，不重写）:
- `__init__()` 方法逻辑基本一致（读环境变量, 创建 OpenAI client, 设置 model）
- `_call()` 方法逻辑基本一致（available 检查, 3 次重试, json_object format, timeout=15s）
- `available` property 逻辑不变

**必须改的部分**:
- `narrate_diagnosis()` 的 Pydantic 验证从 `SearchSpaceResponse` 改为 `DiagnosisNarrative`
- 温度从 0.3 → 0.2

---

## 4. 修改文件（3 个文件，局部改动）

### 4.1 `orchestration/workflow_engine.py` — 6 处局部改动

#### 改动 1：新增 Phase 0（在 Phase 1 之前）

位置: `process()` 方法，`# ====== Phase 1: 诊断 ======` 之前插入：

```python
# ====== Phase 0: 情绪解析 (NL → 结构化目标) ======
emotion_parsed = None
try:
    from moodify.knowledge.emotion_targets import resolve_emotion_from_nl
    emotion_parsed = resolve_emotion_from_nl(emotion_target)
except Exception:
    pass

if emotion_parsed is None:
    emotion_parsed = {
        "emotion_key": "gentle_awakening",
        "emotion_code": "GA",
        "intensity": 0.6,
        "vector_bias": {"E": 0.0, "D": 0.0, "S": 0.0, "T": 0.0, "H": 0.0},
        "source": "fallback",
    }

resolved_emotion_key = emotion_parsed["emotion_key"]
```

然后将 Phase 1 调用：
```python
# 原: phase1 = self._run_diagnosis(input_path, emotion_target)
# 改为:
phase1 = self._run_diagnosis(input_path, resolved_emotion_key)
```

#### 改动 2：重命名 Phase 1.5 方法调用

位置: Phase 1 之后、Phase 2 之前

```python
# 原（SPEC-002）:
# phase1_5 = self._run_param_search(
#     phase1.output.get("wave_state_diagnosis"), emotion_target
# )

# 改为（SPEC-002-REV）:
phase1_5 = self._run_strength_search(
    phase1.output.get("wave_state_diagnosis"),
    emotion_target
)
```

#### 改动 3：替换 Phase 1.5 方法实现

位置: 类的末尾

```python
# 删除原 _run_param_search() 方法
# 替换为 _run_strength_search() 方法
# 内容从 REV 规格书 Section 2.2 修改点 C 复制
```

关键差异: 内部调用从 `search_optimal_params()` → `search_optimal_strengths()`，输出键从 `top_params` → `top_params_list` + `top_strengths`。

#### 改动 4：更新 Phase 1.5 的 fallback 逻辑

位置: 紧接改动 2 之后

```python
# 原 fallback:
# top_params = phase1_5.output.get("top_params", [])
# if not top_params:
#     from moodify.knowledge.craft_chains import get_recommended_params
#     code = resolve_emotion(emotion_target, as_key=True)
#     top_params = [get_recommended_params(code)]

# 改为（修复了 resolve_emotion 的参数错误 + 新增 strength fallback）:
top_params_list = phase1_5.output.get("top_params_list", [])
top_strengths = phase1_5.output.get("top_strengths", [])
top_scores = phase1_5.output.get("top_scores", [])

if not top_params_list:
    from moodify.knowledge.craft_chains import get_recommended_params
    from moodify.knowledge.emotion_targets import resolve_emotion, KEY_TO_CODE
    try:
        key = resolve_emotion(emotion_target)
        code = KEY_TO_CODE.get(key, "GA")
    except Exception:
        code = "GA"
    top_params_list = [get_recommended_params(code)]
    top_strengths = [{"spectrum": 0.5, "dynamic": 0.5, "space": 0.5, "layer": 0.5, "master": 0.5}]
    top_scores = [0.0]
```

#### 改动 5：更新 Phase 3-6 多版本循环

位置: 原 Phase 3 调用之后、Phase 4-6 循环处

```python
# 原（SPEC-002）:
# for i, (params, proxy_score) in enumerate(zip(
#     top_params, phase1_5.output.get("top_scores", [0.0]*len(top_params))
# )):

# 改为:
for i, (params, proxy_score) in enumerate(zip(
    top_params_list,
    top_scores if top_scores else [0.0] * len(top_params_list)
)):
```

循环体内逻辑保持不变。

#### 改动 6：新增 LLM 诊断解读（在 return 之前）

位置: `process()` 方法末尾，`return WorkflowResult(...)` 之前

```python
# 新增:
narrative = None
try:
    from moodify.llm.client import DeepSeekClient
    llm = DeepSeekClient()
    if llm.available and ws_diagnosis and output_path:
        ws_after = self._diagnose_audio(output_path)
        best_params = top_params_list[best_idx] if best_idx < len(top_params_list) else {}
        narrative = llm.narrate_diagnosis(
            before_dict=ws_diagnosis.to_dict(),
            after_dict=ws_after.to_dict() if ws_after else {},
            params=best_params,
            whs_before=whs_before,
            whs_after=whs_after,
            eds=eds,
            emotion_name=emotion_target,
        )
except Exception:
    pass
```

---

### 4.2 `knowledge/emotion_targets.py` — 追加 1 个函数

位置: 文件末尾，`list_all_emotions()` 之后

```python
def resolve_emotion_from_nl(nl_text: str) -> dict:
    """
    自然语言情绪描述 → 结构化情绪目标。
    优先级: 预设alias表 → DeepSeek → 默认fallback
    """
    # Step 1: 尝试预设匹配
    try:
        key = resolve_emotion(nl_text)
        code = KEY_TO_CODE.get(key, "GA")
        return {
            "emotion_key": key,
            "emotion_code": code,
            "intensity": 0.6,
            "vector_bias": {"E": 0.0, "D": 0.0, "S": 0.0, "T": 0.0, "H": 0.0},
            "source": "preset",
        }
    except KeyError:
        pass

    # Step 2: 调 DeepSeek
    try:
        from moodify.llm.client import DeepSeekClient
        client = DeepSeekClient()
        if client.available:
            result = client.interpret_emotion(nl_text)
            if result:
                code = result["emotion_code"]
                key = CODE_TO_KEY.get(code, "gentle_awakening")
                return {
                    "emotion_key": key,
                    "emotion_code": code,
                    "intensity": result["intensity"],
                    "vector_bias": result["vector_bias"],
                    "source": "deepseek",
                }
    except Exception:
        pass

    # Step 3: 回退
    return {
        "emotion_key": "gentle_awakening",
        "emotion_code": "GA",
        "intensity": 0.6,
        "vector_bias": {"E": 0.0, "D": 0.0, "S": 0.0, "T": 0.0, "H": 0.0},
        "source": "fallback",
    }
```

---

### 4.3 `knowledge/craft_chain_match.py` — 回退 DEPRECATED 标记

```python
# 如果文件头有类似以下注释，删除:
# DEPRECATED: replaced by optimizer/search.py

# 确认文件底部 generate_craft_cards_from_data() 函数存在且正常
# 确认 from moodify.knowledge.craft_chain_match import ... 的 import 语句未被注释
```

---

## 5. 执行顺序（9 步）

```
步骤 1: 删除 4 个废弃文件
  └── rm optimizer/params_to_strengths.py optimizer/proxy_metric.py llm/schemas.py llm/prompts.py

步骤 2: 重写 optimizer/search.py
  └── 从 REV 规格书 2.1 节逐节复制

步骤 3: 重写 llm/client.py
  └── 从 REV 规格书 3.1 节逐节复制
  └── 注意: __init__() 和 _call() 可以复用原实现

步骤 4: 修改 workflow_engine.py — 改动 1（新增 Phase 0）

步骤 5: 修改 workflow_engine.py — 改动 2+3+4（替换 Phase 1.5）

步骤 6: 修改 workflow_engine.py — 改动 5（更新循环变量名）

步骤 7: 修改 workflow_engine.py — 改动 6（新增 LLM 诊断解读）

步骤 8: 追加 emotion_targets.py — resolve_emotion_from_nl()

步骤 9: 回退 craft_chain_match.py — 删除 DEPRECATED 标记
```

---

## 6. 验证

```bash
cd moodify-core-package
pip install -e .

# 导入验证 — 确认旧模块已删除, 新接口存在
python -c "
from moodify.optimizer.search import search_optimal_strengths, strength_to_params
from moodify.llm.client import DeepSeekClient
from moodify.knowledge.emotion_targets import resolve_emotion_from_nl
print('All new imports OK')
"

# 确认旧接口不可用（如果 SPEC-002 的调用方还存在，应该报 ImportError）
python -c "
try:
    from moodify.optimizer.params_to_strengths import params_to_strengths
    print('ERROR: old module should be deleted')
except ImportError:
    print('params_to_strengths deleted: OK')
try:
    from moodify.optimizer.proxy_metric import compute_edsr_proxy
    print('ERROR: old module should be deleted')
except ImportError:
    print('proxy_metric deleted: OK')
"

# craft_chain_match 验证 — 确认未被废弃
python -c "
from moodify.knowledge.craft_chain_match import CraftChainMatch, generate_craft_cards_from_data
cards = generate_craft_cards_from_data()
assert len(cards) == 8
print(f'craft_chain_match active: {len(cards)} cards')
"

# 5D 搜索功能验证
python -c "
from moodify.optimizer.search import strength_to_params
from moodify.knowledge.craft_chains import get_recommended_params, PARAM_KEYS
sv = {'spectrum':0.5, 'dynamic':0.5, 'space':0.5, 'layer':0.5, 'master':0.5}
params = strength_to_params(sv, 'GA')
rec = get_recommended_params('GA')
for k in PARAM_KEYS:
    assert abs(params[k] - rec[k]) < 0.05, f'{k}: {params[k]:.2f} vs {rec[k]:.2f}'
print('strength_to_params @ 0.5 == rec: OK')
"

# DeepSeek fallback 验证 (不设置 API key)
python -c "
from moodify.llm.client import DeepSeekClient
c = DeepSeekClient()
assert not c.available
assert c.interpret_emotion('任意文本') is None
assert c.narrate_diagnosis({}, {}, {}, 0, 0, 0, '') is None
print('DeepSeek fallback OK')
"

# 情绪解析 fallback 验证
python -c "
from moodify.knowledge.emotion_targets import resolve_emotion_from_nl
r = resolve_emotion_from_nl('像清晨阳光穿过窗帘')
assert r['emotion_code'] == 'GA'  # fallback
assert r['source'] == 'fallback'
print('NL emotion fallback OK')
"
```

---

## 7. 附录：SPEC-002 产出中可以安全跳过的步骤

如果 SPEC-002 执行过程中以下步骤尚未完成，直接跳过，不要补做：

```
□ optimizer/params_to_strengths.py  ← 不要做, REV 不需要
□ optimizer/proxy_metric.py         ← 不要做, REV 不需要
□ llm/schemas.py                    ← 不要做, REV 不需要
□ llm/prompts.py                    ← 不要做, REV 不需要
□ llm/client.py 的 define_search_space() ← 不要做, 改写为 interpret_emotion()
□ workflow_engine.py 的 _run_param_search() ← 不要做, 改写为 _run_strength_search()
□ craft_chain_match.py 的 DEPRECATED 标记  ← 不要加, REV 需要保留
```

如果 SPEC-002 执行过程中以下步骤已完成，保留不动：

```
✓ memory/history.py                  ← 保留, 完整的资产
✓ memory/__init__.py                 ← 保留
✓ optimizer/__init__.py              ← 保留
✓ llm/__init__.py                    ← 保留
✓ pip install openai>=1.0            ← 已完成, 不再需要
✓ workflow_engine.py 的 Phase 3 多版本循环框架 ← 保留框架, 修改变量名
```
