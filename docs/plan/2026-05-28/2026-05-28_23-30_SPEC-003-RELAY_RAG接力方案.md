# SPEC-003-RELAY: 接力方案 — 在 SPEC-002-REV 基础上添加 RAG 层

**时间**: 2026-05-28 23:30
**目标读者**: 第二个 AI 编码智能体（在第一个 AI 完成 SPEC-002-REV 后启动）
**前置**: SPEC-002-REV 全部完成且验证通过
**定位**: 增量添加，不推翻，不重写

---

## 0. 接力逻辑

SPEC-002-REV 完成了核心引擎：
- 5D 强度空间 LHS 搜索
- NL → 情绪映射（DeepSeek `interpret_emotion`）
- 多版本 DSP + 选最优
- 诊断解读（DeepSeek `narrate_diagnosis`）

SPEC-002-REV **没有做**的是：
- 记忆系统（阶段 C 推迟了，需要 ≥50 条记录）
- 把相似案例注入 LLM prompt（RAG 前置检索）
- 离线加权平均降级
- 历史记录写入

**本接力方案：在 SPEC-002-REV 的代码之上，新增 3 个文件 + 修改 2 个位置。**

---

## 1. 环境快照（接力起点）

SPEC-002-REV 完成后，目录结构为：

```
src/moodify/
├── optimizer/
│   ├── __init__.py
│   └── search.py                  # search_optimal_strengths(), strength_to_params(), 等
├── llm/
│   ├── __init__.py
│   └── client.py                  # DeepSeekClient + Pydantic + Prompts
├── diagnosis/      (不变)
├── processing/     (不变)
├── knowledge/
│   ├── craft_chains.py            (不变)
│   ├── craft_chain_match.py       (保留, proxy_evaluate Step4 使用)
│   └── emotion_targets.py         (已加 resolve_emotion_from_nl())
└── orchestration/
    └── workflow_engine.py         (Phase 0 + Phase 1.5 + 多版本DSP + LLM解读)
```

已有公开接口（本接力要调用的）：

| 来自 | 接口 | 返回值 |
|------|------|--------|
| `optimizer/search.py` | `search_optimal_strengths(diagnosis, emotion, top_k)` | `list[(vec, params, score)]` |
| `optimizer/search.py` | `strength_to_params(vec, emotion_code)` | `dict[str, float]` (15参数) |
| `llm/client.py` | `DeepSeekClient().available` | `bool` |
| `llm/client.py` | `DeepSeekClient().recommend_params(prompt)` | **不存在 — SPEC-002-REV 没有这个接口** |
| `llm/client.py` | `DeepSeekClient().interpret_emotion(nl)` | `dict \| None` |
| `llm/client.py` | `DeepSeekClient().narrate_diagnosis(...)` | `dict \| None` |
| `diagnosis/engine.py` | `DiagnosisEngine().diagnose_quick(path)` | `WaveStateDiagnosis` |
| `diagnosis/defect_classifier.py` | `DefectClassifier().classify(ws, emotion)` | `list[Defect]` |
| `knowledge/emotion_targets.py` | `resolve_emotion(name)` | `str` |
| `knowledge/emotion_targets.py` | `EMOTION_TARGETS_V2` | `dict` |
| `data_types.py` | `WaveStateDiagnosis.to_dict()` | `dict` |

---

## 2. 新增文件（3 个）

### 2.1 `memory/__init__.py`

空文件。

### 2.2 `memory/history.py`

```python
"""处理记录存储 + 余弦相似度检索 + 时间衰减"""
import json, math, numpy as np
from dataclasses import dataclass, asdict
from pathlib import Path
from datetime import datetime


@dataclass
class ProcessingRecord:
    diagnosis_vector: list[float]  # 14自动参数 → L2归一化 (排除E1/E2主观参数)
    params: dict[str, float]       # 15 DSP参数
    strength_vector: dict[str, float]  # 5D强度值
    whs_before: float
    whs_after: float
    eds: float
    proxy_score: float
    emotion_code: str
    emotion_name: str
    user_intent: str               # "" 表示自动推断
    satisfied: bool | None         # None=无反馈, True=满意, False=不满意
    user_feedback: str             # "" 表示无文字反馈
    timestamp: str                 # ISO 8601


class ProcessingHistory:

    def __init__(self, storage_dir: str = "outputs"):
        self._path = Path(storage_dir) / "processing_history.jsonl"

    def save(self, record: ProcessingRecord) -> None:
        self._path.parent.mkdir(parents=True, exist_ok=True)
        with open(self._path, "a", encoding="utf-8") as f:
            f.write(json.dumps(asdict(record), ensure_ascii=False) + "\n")

    def load_all(self) -> list[ProcessingRecord]:
        if not self._path.exists():
            return []
        records = []
        with open(self._path, "r", encoding="utf-8") as f:
            for line in f:
                try:
                    records.append(ProcessingRecord(**json.loads(line)))
                except Exception:
                    pass
        return records

    def find_similar(
        self, query_vector: list[float], top_k: int = 5
    ) -> list[tuple[ProcessingRecord, float]]:
        """余弦相似度 + 时间衰减。半衰期 180 天。跳过 >365 天的记录。"""
        records = self.load_all()
        if not records:
            return []

        now = datetime.now()
        q = np.array(query_vector, dtype=np.float64)
        scored = []
        for r in records:
            try:
                age_days = (now - datetime.fromisoformat(r.timestamp)).days
            except Exception:
                age_days = 0
            if age_days > 365:
                continue

            v = np.array(r.diagnosis_vector, dtype=np.float64)
            cos_sim = float(np.dot(q, v) / (np.linalg.norm(q) * np.linalg.norm(v) + 1e-8))
            time_decay = math.exp(-age_days / 180.0)
            scored.append((r, cos_sim * time_decay))

        scored.sort(key=lambda x: x[1], reverse=True)
        return scored[:top_k]

    def count(self) -> int:
        return len(self.load_all())


def diagnosis_to_vector(diagnosis) -> list[float]:
    """WaveStateDiagnosis → L2归一化向量。排除布尔型 SP4 和主观参数 E1/E2。"""
    d = diagnosis.to_dict()
    vec = []
    for dim_name in ["Spectrum", "Dynamics", "Space", "Layers", "Emotion"]:
        for key, val in d[dim_name].items():
            if key in ("SP4_WidthHealth",):  # 布尔值
                continue
            if key in ("E1_Direction", "E2_Richness"):  # 主观参数
                continue
            vec.append(float(val) if not isinstance(val, bool) else (1.0 if val else 0.0))
    arr = np.array(vec, dtype=np.float64)
    norm = np.linalg.norm(arr)
    return (arr / norm).tolist() if norm > 1e-8 else arr.tolist()
```

**验收**:
```python
from moodify.memory.history import ProcessingHistory, ProcessingRecord

h = ProcessingHistory("outputs")
h.save(ProcessingRecord(
    diagnosis_vector=[0.1]*14, params={"P01_vocal_presence_freq": 3000},
    strength_vector={"spectrum": 0.5, "dynamic": 0.5, "space": 0.5, "layer": 0.5, "master": 0.5},
    whs_before=70, whs_after=80, eds=65, proxy_score=72.0,
    emotion_code="GA", emotion_name="温柔觉醒",
    user_intent="", satisfied=None, user_feedback="",
    timestamp="2026-05-28T23:00:00",
))
assert h.count() >= 1
```

### 2.3 `llm/prompt_assembler.py`

```python
"""组装 RAG-enhanced prompt: 诊断 + 案例 + 意图 → 发给 DeepSeek"""
import json


def assemble_rag_prompt(
    diagnosis_dict: dict,
    defects_list: list[dict],
    emotion_name: str,
    user_intent: str,
    similar_cases: list[dict],  # [{"similarity":0.92, "params":{...}, "whs_delta":9, "satisfied":True, "feedback":"满意"}, ...]
) -> str:
    """组装发给 DeepSeek 的参数推荐 prompt, 包含相似案例。

    similar_cases 格式来自 ProcessingHistory.find_similar() 的结果,
    经由 _format_cases() 转换。
    """
    parts = []

    parts.append("## 当前诊断\n")
    parts.append("```json\n" + json.dumps(diagnosis_dict, ensure_ascii=False, indent=2) + "\n```\n")

    if defects_list:
        parts.append("## 检测到的缺陷\n")
        for d in defects_list:
            parts.append(f"- {d['parameter']}: severity={d['severity']}\n")

    parts.append(f"\n## 目标情绪\n{emotion_name}\n")

    if user_intent:
        parts.append(f"\n## 用户意图\n{user_intent}\n")
    else:
        parts.append(f"\n## 用户意图\n自动推断: 向{emotion_name}方向处理\n")

    if similar_cases:
        parts.append(f"\n## 相似案例 (共{len(similar_cases)}个, 来自本地处理历史)\n")
        for i, case in enumerate(similar_cases):
            sim = case.get("similarity", 0)
            params = case.get("params", {})
            feedback = case.get("feedback", "")
            whs_d = case.get("whs_delta", 0)
            satisfied = case.get("satisfied", True)
            strength = case.get("strength_vector", {})

            status = "满意" if satisfied else "不满意"
            parts.append(f"### 案例 {i+1} (相似度: {sim:.2f}, 用户{status})\n")
            parts.append(f"WHS变化: +{whs_d}\n")
            if feedback:
                parts.append(f"用户反馈: {feedback}\n")
            if strength:
                parts.append(f"5D强度: spectrum={strength.get('spectrum',0):.2f}, dynamic={strength.get('dynamic',0):.2f}, space={strength.get('space',0):.2f}, layer={strength.get('layer',0):.2f}\n")

            non_zero = {k: round(v,2) for k, v in params.items() if abs(v) > 0.01}
            if non_zero:
                parts.append("使用的参数:\n")
                for k, v in list(non_zero.items())[:8]:
                    parts.append(f"  {k} = {v}\n")
            parts.append("\n")
    else:
        parts.append("\n## 相似案例\n无 (知识库为空, 请基于诊断数据推理)\n")

    parts.append("请基于以上信息, 输出推荐的处理强度和参数。输出严格的 JSON 格式。\n")
    return "".join(parts)


def format_cases_for_prompt(
    similar: list[tuple],  # list[(ProcessingRecord, float)]
) -> list[dict]:
    """将 ProcessingHistory.find_similar() 的输出格式化为 prompt_assembler 可用的 dict 列表。"""
    cases = []
    for rec, sim in similar:
        cases.append({
            "similarity": round(sim, 3),
            "params": rec.params,
            "strength_vector": rec.strength_vector,
            "whs_delta": round(rec.whs_after - rec.whs_before, 1),
            "satisfied": rec.satisfied is None or rec.satisfied,
            "feedback": rec.user_feedback or "",
        })
    return cases
```

### 2.4 `llm/offline_fallback.py`

```python
"""离线降级: API 不可用时加权平均相似案例参数"""
from moodify.knowledge.craft_chains import get_recommended_params

PARAM_KEYS_15 = [
    "P01_vocal_presence_freq", "P02_vocal_presence_gain", "P03_vocal_presence_q",
    "P04_proximity_low_freq", "P05_proximity_low_gain",
    "P06_compression_ratio", "P07_compression_attack", "P08_compression_release", "P09_compression_threshold",
    "P10_reverb_t60", "P11_reverb_dry_wet", "P12_reverb_width",
    "P13_harmonic_drive",
    "P14_high_shelf_freq", "P15_high_shelf_gain",
]

STRENGTH_KEYS = ["spectrum", "dynamic", "space", "layer", "master"]


def aggregate_params_offline(
    similar_cases: list[tuple],  # list[(ProcessingRecord, float)]
    emotion_code: str,
) -> tuple[dict[str, float], dict[str, float]]:
    """加权平均聚合。用户不满意的案例排除。

    Returns:
        (params_dict, strength_vector)
    """
    if not similar_cases:
        rec = get_recommended_params(emotion_code)
        default_sv = {k: 0.5 for k in STRENGTH_KEYS}
        return rec, default_sv

    valid = [(rec, sim) for rec, sim in similar_cases if rec.satisfied is None or rec.satisfied]
    if not valid:
        rec = get_recommended_params(emotion_code)
        return rec, {k: 0.5 for k in STRENGTH_KEYS}

    total_weight = sum(sim for _, sim in valid)

    params = {}
    for key in PARAM_KEYS_15:
        wsum = 0.0
        for rec, sim in valid:
            wsum += rec.params.get(key, 0.0) * sim
        params[key] = wsum / total_weight

    strength = {}
    for key in STRENGTH_KEYS:
        wsum = 0.0
        for rec, sim in valid:
            wsum += rec.strength_vector.get(key, 0.5) * sim
        strength[key] = wsum / total_weight

    return params, strength
```

---

## 3. 修改文件（2 处）

### 3.1 `llm/client.py` — 追加 `recommend_params` 方法

在 `DeepSeekClient` 类中追加（SPEC-002-REV 的 `llm/client.py` 没有这个方法）:

```python
def recommend_params(self, prompt: str) -> dict | None:
    """RAG-enhanced 参数推荐。输入是 assemble_rag_prompt() 的输出。
    
    Returns:
        {
            "parameters": [{"param_name":"P01_...", "value":3000.0, "reasoning":"..."}, ...],
            "strength_vector": {"spectrum":0.52, ...},
            "confidence": 0.82,
        }
        失败返回 None
    """
    RECOMMENDER_SYSTEM = """\
你是 Moodify 情绪波场显影引擎的参数推荐师。

基于当前诊断数据、目标情绪、用户意图和相似历史案例，推荐处理参数。

## 输出格式 (严格 JSON)
{
  "parameters": [
    {"param_name": "P01_vocal_presence_freq", "value": 3000.0},
    ... 共15个, 必须全部包含
  ],
  "strength_vector": {"spectrum":0.52, "dynamic":0.48, "space":0.45, "layer":0.50, "master":0.50},
  "confidence": 0.82,
  "reasoning": "推理过程, 100字以内中文",
  "reference_case_ids": [1, 3]
}

## 规则
- 如果有相似案例, 案例参数是主要参考。需要根据当前诊断的差异做调整。
- 用户不满意的案例要反向参考。
- 所有值必须在安全范围内。
"""
    return self._call(RECOMMENDER_SYSTEM, prompt)
```

### 3.2 `orchestration/workflow_engine.py` — 两处修改

**修改 1**: 在 `process()` 方法的 Phase 1.5（强度搜索）之前，插入 RAG 尝试:

在 `# ====== Phase 1.5: 5D 强度空间搜索 ======` 这一行**之前**插入:

```python
# ====== Phase 1.3: RAG 检索 + LLM 推荐 (如有历史案例) ======
rag_params = None
rag_strength = None
rag_source = None
rag_confidence = 0.0

try:
    from moodify.memory.history import ProcessingHistory, diagnosis_to_vector
    from moodify.llm.client import DeepSeekClient

    history = ProcessingHistory(self._output_dir)
    if history.count() > 0:
        dvec = diagnosis_to_vector(ws_diagnosis)
        similar = history.find_similar(dvec, top_k=5)
        top1_sim = similar[0][1] if similar else 0.0

        llm = DeepSeekClient()
        if llm.available and top1_sim > 0.5:
            # 组装 RAG prompt
            from moodify.llm.prompt_assembler import assemble_rag_prompt, format_cases_for_prompt
            from moodify.diagnosis.defect_classifier import DefectClassifier

            classifier = DefectClassifier()
            defects = classifier.classify(ws_diagnosis, resolved_emotion_key)
            defects_list = [{"parameter": d.parameter, "severity": d.severity} for d in defects]

            cases_formatted = format_cases_for_prompt(similar)
            prompt = assemble_rag_prompt(
                ws_diagnosis.to_dict(), defects_list,
                emotion_parsed.get("emotion_name", emotion_target),
                "",  # user_intent — 当前 process() 接口没有 user_intent 参数, 先用空
                cases_formatted,
            )

            result = llm.recommend_params(prompt)
            if result and "parameters" in result and "strength_vector" in result:
                rag_params = {p["param_name"]: p["value"] for p in result["parameters"]}
                rag_strength = result["strength_vector"]
                rag_confidence = result.get("confidence", 0.7)
                rag_source = "rag_llm"
except Exception:
    pass
```

然后在 `# ====== Phase 1.5: 5D 强度空间搜索 ======` 处改为:

```python
# ====== Phase 1.5: 参数确定 ======
# 优先使用 RAG 结果, 否则使用搜索
if rag_params is not None and rag_confidence > 0.7:
    top_params_list = [rag_params]
    top_strengths = [rag_strength]
    top_scores = [rag_confidence * 100.0]
    phase1_5 = PhaseResult(
        phase=1.5, name="参数推荐(RAG)",
        status=PhaseStatus.COMPLETED,
        output={
            "top_params_list": top_params_list,
            "top_strengths": top_strengths,
            "top_scores": top_scores,
            "source": "rag_llm",
        },
        elapsed_ms=0,
    )
else:
    # Fallback: 原有强度搜索逻辑 (SPEC-002-REV)
    phase1_5 = self._run_strength_search(
        phase1.output.get("wave_state_diagnosis"),
        emotion_target,
    )
    top_params_list = phase1_5.output.get("top_params_list", [])
    top_strengths = phase1_5.output.get("top_strengths", [])
    top_scores = phase1_5.output.get("top_scores", [])

    if not top_params_list:
        from moodify.knowledge.craft_chains import get_recommended_params
        from moodify.knowledge.emotion_targets import resolve_emotion, KEY_TO_CODE
        try:
            code = resolve_emotion(emotion_target)
            code = KEY_TO_CODE.get(code, "GA")
        except Exception:
            code = "GA"
        top_params_list = [get_recommended_params(code)]
        top_strengths = [{"spectrum": 0.5, "dynamic": 0.5, "space": 0.5, "layer": 0.5, "master": 0.5}]
        top_scores = [0.0]

phases.append(phase1_5)
```

**修改 2**: 在 `process()` 方法的 `return WorkflowResult(...)` 之前，插入历史记录:

```python
# ====== 记录处理历史 ======
try:
    from moodify.memory.history import ProcessingHistory, ProcessingRecord, diagnosis_to_vector

    h = ProcessingHistory(self._output_dir)
    h.save(ProcessingRecord(
        diagnosis_vector=diagnosis_to_vector(ws_diagnosis),
        params=best_params if best_params else (top_params_list[0] if top_params_list else {}),
        strength_vector=best_strength if best_strength else (top_strengths[0] if top_strengths else {}),
        whs_before=whs_before,
        whs_after=whs_after,
        eds=eds,
        proxy_score=float(top_scores[best_idx]) if top_scores and best_idx < len(top_scores) else 0.0,
        emotion_code=emotion_parsed["emotion_code"],
        emotion_name=emotion_parsed.get("emotion_name", emotion_target),
        user_intent="",
        satisfied=None,
        user_feedback="",
        timestamp=datetime.now().isoformat(),
    ))
except Exception:
    pass
```

---

## 4. SPEC-002-REV 与接力后的对比

| | SPEC-002-REV 完成时 | 接力完成后 |
|---|---|---|
| 参数来源 | 搜索 (LHS + 代理 + 真实DSP选优) | RAG优先 → 搜索回退 |
| 是否有记忆 | 无 (阶段C推迟) | 有 (立即记录, 立即检索) |
| LLM 角色 | NL→情绪 + 诊断解读 | + 参数推荐 (RAG增强) |
| 离线可用性 | 降级到搜索 | 降级到加权平均 → 搜索 |
| 越用越好 | 否 | 是 (每次处理丰富知识库) |
| 新增文件 | 3 | +4 (memory/history, llm/prompt_assembler, llm/offline_fallback, memory/__init__) |
| 修改文件 | — | +2 (llm/client.py 加方法, workflow_engine.py 加2处) |

---

## 5. 执行顺序

```
01. 确认 SPEC-002-REV 全部完成且 smoke_test.py 通过
02. mkdir memory/ + 创建 memory/__init__.py (空)
03. 创建 memory/history.py               → 验收 (save/load)
04. 创建 llm/prompt_assembler.py         → 验收 (prompt 长度 > 100)
05. 创建 llm/offline_fallback.py         → 验收 (聚合输出 15 参数)
06. 修改 llm/client.py                   → 追加 recommend_params 方法
07. 修改 orchestration/workflow_engine.py → 修改1 (Phase1.3) + 修改2 (history记录)
08. smoke_test.py → CLI process 完整流程
09. 处理 2 首不同歌曲 → 确认 history.jsonl 增长
10. 处理第 1 首歌曲的副本 → 确认 source="rag_llm" (案例召回命中)
11. 清除 DEEPSEEK_API_KEY → 确认降级到搜索, 不崩溃
```

---

## 6. 验证命令

```bash
# 确认 memory 模块可用
python -c "from moodify.memory.history import ProcessingHistory, ProcessingRecord; print('OK')"

# 确认 prompt_assembler 可用
python -c "from moodify.llm.prompt_assembler import assemble_rag_prompt; p=assemble_rag_prompt({'S':{}},[],'GA','',[]); assert len(p)>100; print('OK')"

# 确认 offline_fallback 可用
python -c "from moodify.llm.offline_fallback import aggregate_params_offline; p,s=aggregate_params_offline([],'GA'); assert len(p)==15; print('OK')"

# 确认 DeepSeekClient 有 recommend_params 方法
python -c "from moodify.llm.client import DeepSeekClient; assert hasattr(DeepSeekClient,'recommend_params'); print('OK')"

# 端到端: 第一次处理 (无历史, 走搜索)
python -c "
from moodify.orchestration.workflow_engine import WorkflowOrchestrator
w = WorkflowOrchestrator()
r = w.process('test.wav', '温柔觉醒')
print(f'Source: {r.phases}' if hasattr(r,'phases') else 'done')
"

# 第二次处理同一首歌 (有历史, 走 RAG) — 需要 DEEPSEEK_API_KEY
$env:DEEPSEEK_API_KEY="sk-xxx"
python -c "
from moodify.orchestration.workflow_engine import WorkflowOrchestrator
w = WorkflowOrchestrator()
r = w.process('test.wav', '温柔觉醒')
# 观察日志: 应该有 '参数推荐(RAG)' phase, source='rag_llm'
"
```

---

## 7. 不做的（留给第三棒）

- 用户意图输入 (当前 process() 接口没有 `user_intent` 参数, 第二棒先用空字符串)
- A/B 反馈的 `satisfied` 字段更新 (当前只写入, 不更新)
- ≥50 条记录后的代理模型重校准
- 前端 UI 的 RAG 状态展示
