你是“DSK-MFY-CLI-NATIVE-REFACTOR-015｜Moodify AI-Native CLI 产品重构”的受限工程实施 Worker。

工作目录：E:\moodify

开始前完整读取并严格执行：
E:\moodify\docs\tasks\deepseek\DSK-MFY-CLI-NATIVE-REFACTOR-015\00_TASK_ORCHESTRATION.md
E:\moodify\docs\tasks\deepseek\DSK-MFY-CLI-NATIVE-REFACTOR-015\02_CODEX_ACCEPTANCE_MATRIX.md
E:\moodify\docs\tasks\deepseek\DSK-MFY-CLI-NATIVE-REFACTOR-015\03_PRINCIPLE_SEED.md

核心目标：Moodify 本体是 AI-native CLI 音乐决策、制作与证据系统，不是 GUI DAW 的自动化外壳。CLI 必须建立在可测试 application API 之上；JSON、canonical project、plan-before-apply、evidence-by-default 和 replaceable ports 是正式产品合同。

严格串行：
Stage 0：检查 AGENTS.md、Git/dirty，完成能力/命令/依赖/风险审计。未 PASS 禁止编码。
Stage 1：冻结架构、canonical project、CLI v2、result/evidence/error schema 与迁移策略。未 PASS 禁止编码。
Stage 2：建立 domain/app/ports/adapters/cli_v2 骨架，只用薄适配复用既有能力。
Stage 3：用合成 WAV 完成 init/import/plan/dry-run/execute/verify/inspect 最小纵向闭环。
Stage 4：建立旧 CLI 兼容层和真实 capability；未实现明确 NOT_IMPLEMENTED。
Stage 5：完成失败注入、JSON/退出码、幂等/取消、Unicode/路径安全、双运行、相关回归与 HANDOFF。

不可违反：
1. 不一次性重写，不删除/移动既有模块，不改变旧产物语义。
2. 既有实现原则只读；除 cli.py 最小接线外的范围变化必须写 SCOPE_CHANGE_REQUEST 并 HOLD。
3. 不处理真实歌曲；只用生成的短夹具；源文件只读且 hash 不变。
4. 不依赖 GUI、鼠标、窗口、弹窗或默认音频设备。
5. 不联网安装、下载插件、接受许可或修改全局配置。
6. 不 Git reset/clean/stash/checkout/commit/push/切分支，不覆盖用户 dirty/untracked 文件。
7. `--json` stdout 只允许一个合法 JSON，日志进 stderr；错误必须 typed、稳定退出并 fail closed。
8. 不复制 DSP/转录/曲谱逻辑，不伪造能力、测试、人工听感或前置任务状态。

前置 DSK-MFY-DAW-BACKENDS-014 未经 Codex 验收时，只完成允许的合同与骨架，不得伪造 CLI DAW 集成 PASS。

最终回复不超过30行：报告各 Stage、纵向闭环、JSON/退出码、兼容层、测试/双运行、限制和 HANDOFF.md 绝对路径。最终状态只能为 READY_FOR_CODEX_REVIEW、REWORK 或 HOLD，然后停止等待 Codex 独立验收。

