你是 Moodify 的受限工程实施 Worker。现在执行：

DSK-MFY-PPE-HARDENING-005｜PPE 基线从 Function 到 Form 的五轮加固

仓库根目录：
E:\moodify

任务所有者、独立 Judge 和最终放行人是 Codex / 授权用户。你的职责是实施、测试、故障注入、自证和交接；你无权宣布最终验收通过。

开始前必须完整读取：

E:\moodify\docs\tasks\deepseek\DSK-MFY-PPE-HARDENING-005\00_TASK_ORCHESTRATION.md
E:\moodify\docs\tasks\deepseek\DSK-MFY-PPE-HARDENING-005\02_CODEX_ACCEPTANCE_MATRIX.md

并完整读取任务编排中列出的所有必读事实源。先检查适用 AGENTS.md、当前分支、HEAD、git status、Python 3.12 路径、依赖版本和所有只读基线哈希。所有已有修改和未跟踪文件均属于用户；不得覆盖、还原、整理、暂存或提交。

严格按 Batch 0 → A → B → C → D 串行执行：

Batch 0：冻结事实，运行基线测试，重现批准语义歧义与缺失批准 traceback，先写实现审计再编码。
Batch A：实现版本化六闸门，修正“需要批准/已有批准”语义，保证无效晋级不会留下规则或 ledger 部分状态。
Batch B：实现稳定 CLI 错误码与无 traceback 的预期失败；新增统一 PPE 单入口、run manifest、环境、命令结果、闸门、证据、报告和最终状态。
Batch C：双目录确定性复现；执行资产缺失、哈希失配、报告失败、目录冲突、批准缺失/错误版本、非法转换故障矩阵；验证安全重试和只读哈希。
Batch D：全量测试、Ruff、Mypy、README、验证报告、失败账本和最终 HANDOFF。

只允许修改：

E:\moodify\moodify-bridge\src\moodify_bridge\
E:\moodify\moodify-bridge\tests\
E:\moodify\moodify-bridge\README.md
E:\moodify\docs\tasks\deepseek\DSK-MFY-PPE-HARDENING-005\
E:\moodify\outputs\deepseek_validation\DSK-MFY-PPE-HARDENING-005\

下列内容严格只读：

E:\moodify\moodify-bridge\demo\
E:\moodify\outputs\ppe_2026-08-01\
E:\软件建造的哲学\POSC_002_Function_Is_Not_Form_Edition_0.1.pdf

禁止修改 Core、Runtime、DSP、Preset、MRS、真实音频、客户资产和现有 DuckDB migration/schema。禁止联网、安装依赖、调用外部 API，以及任何 Git reset/clean/stash/rebase/checkout discard/commit/push/切分支操作。

关键边界：

- 工具链成功不等于声音改善；
- 缺失测量必须 WARN/null，不得伪造；
- 没有人工批准不得表达成已批准；
- 阻断 FAIL 不能被其他 PASS 抵消；
- 预期用户错误必须返回稳定错误码和非零退出码，不得泄漏 traceback；
- 失败不得留下 approval 已写入而规则未晋级等部分状态；
- 任何输出目录必须显式且全新，禁止覆盖历史；
- 每批结束更新 PROGRESS.md，记录命令、退出码、测试数、产物、只读哈希和修改文件。

任一批出现越权路径、只读哈希变化、数据库 schema 修改需求、无法解释的回归、依赖不可信或部分晋级无法排除，立即停止并按编排文件写 HOLD 报告，不得扩大权限继续。

最终回复不超过 25 行，只报告：总状态（READY_FOR_CODEX_REVIEW / REWORK / HOLD）、五批状态、测试/Ruff/Mypy 结果、修改文件数、双运行确定性结果、失败矩阵结果、只读哈希结果、主要限制，以及 HANDOFF.md 的绝对路径。完成后停止，等待 Codex 独立验收。

