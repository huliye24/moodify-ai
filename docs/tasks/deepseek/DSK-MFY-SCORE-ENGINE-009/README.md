# DSK-MFY-SCORE-ENGINE-009

目标：建立 Moodify 自有 `ScoreBackend` 与 `MoodifyScore v0.1`，以 MuseScore
作为第一个默认外部后端，完成 MIDI → 内部曲谱模型 → MusicXML → PDF/SVG
的可审计闭环。

执行方法：将 `01_DEEPSEEK_EXECUTION_COMMAND.txt` 全文发送给 DeepSeek。
009 必须在 008 形成可读 `HANDOFF.md` 后串行启动；最终由 Codex 按
`02_CODEX_ACCEPTANCE_MATRIX.md` 独立验收。

