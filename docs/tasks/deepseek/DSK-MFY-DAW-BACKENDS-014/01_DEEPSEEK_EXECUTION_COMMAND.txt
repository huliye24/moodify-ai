你是“DSK-MFY-DAW-BACKENDS-014｜Moodify CLI DAW Engine 最小闭环”的受限工程实施 Worker。

工作目录：E:\moodify

开始前必须完整读取并严格执行：
E:\moodify\docs\tasks\deepseek\DSK-MFY-DAW-BACKENDS-014\00_TASK_ORCHESTRATION.md
E:\moodify\docs\tasks\deepseek\DSK-MFY-DAW-BACKENDS-014\02_CODEX_ACCEPTANCE_MATRIX.md
E:\moodify\docs\tasks\deepseek\DSK-MFY-DAW-BACKENDS-014\03_PRINCIPLE_SEED.md

核心纠偏：Moodify 需要真正的 CLI/headless DAW，不需要用脚本点击 GUI。MoodifyCLIDAWEngine 是主路径；NativeDSP + FFmpeg 是第一执行层。REAPER、Ardour、Audacity、Audition 降为 exporter/handoff compatibility slots，不得作为任务完成前提，不得把带启动参数的 GUI 包装成 CLI DAW。

严格串行执行：
Stage 0：检查 AGENTS.md、Git/dirty、现有 DSP/CLI/证据合同和本机 headless 工具；写实施审计、六份合同、候选引擎矩阵与 Gate。未 PASS 禁止编码。
Stage 1：实现严格类型的 CLI DAW project/timeline/routing/processing/automation/render/evidence 模型。
Stage 2：实现复用既有能力的 NativeDSPBackend 与 FFmpegBackend，完成无界面、多轨、bus/master、离线渲染和可回读证据闭环。
Stage 3：实现 engines/validate/plan/render/verify 五个 CLI；必须能在没有桌面会话和默认音频设备时运行。
Stage 4：执行合成夹具、失败注入、Unicode 路径、双运行、源 hash、相关回归和静态检查，交付 HANDOFF。

不可违反：
1. 不使用鼠标、键盘、窗口焦点、GUI 宏或弹窗。
2. 不联网安装、不下载插件、不接受第三方许可、不修改全局应用配置。
3. 原文件只读，所有输出进入全新目录；现有用户修改不得覆盖、整理、还原、暂存或提交。
4. 项目 JSON/YAML 与 render evidence 是事实源，外部 DAW 工程不是。
5. 子进程必须使用参数数组；超时、非零退出、缺失能力和验证失败必须 fail closed。
6. 只使用生成的短音频夹具；不得处理真实歌曲，不得伪造人工听感。
7. 未实现 adapter 返回 NOT_IMPLEMENTED，不得制造假后端或假 PASS。

最终回复不超过30行，只报告各 Stage、五个 CLI、NativeDSP/FFmpeg 闭环、测试与双运行、限制及 HANDOFF.md 绝对路径。最终状态只能是 READY_FOR_CODEX_REVIEW、REWORK 或 HOLD，然后停止等待 Codex 独立验收。
