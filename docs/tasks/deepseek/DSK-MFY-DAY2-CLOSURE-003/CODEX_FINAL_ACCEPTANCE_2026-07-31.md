# DSK-MFY-DAY2-CLOSURE-003｜Codex 最终独立验收

**日期：2026-07-31**  
**最终判定：ACCEPT / DAY 2 CLOSED**  
**声音结果：TECHNICAL FAIL（dynamic_damage）**  
**人工听感：PENDING**

## 1. 独立结论

DeepSeek 的整体 `PASS` 可以作为证据审查结论接受，但 Codex 不接受其对盲听评分语义完全无歧义的判断。原评分卡未明确说明维度分数是“B 相对于 A”，现已补正。`DAY 2 PASS` 仅表示验证集、权利门禁、协议和单样本证据链闭合，不表示 VS-001 声音通过。

## 2. 已独立确认

- 5 首源文件路径与 SHA-256 一致；
- VSR-001 Markdown/JSON 一致；
- 原 Process manifest 的 5 个产物完整且哈希一致；
- DeepSeek 隔离重放得到字节级一致的主 Process WAV；
- 13 项核心验证字段一致；
- VS-001 保持 `passed=false`、`dynamic_damage`、动态范围 `-7.61 dB`；
- MRS proxy `+24.82` 未被用于推翻硬失败；
- Human feedback 保持 `PENDING`；
- A/B 映射可以独立复算，A/B 音频身份哈希正确；
- 原始源文件在重放后未变化。

## 3. Codex 决策

### 响度门限

门限正式解释为 `|Δ loudness| ≤ 0.20 dB`。一位小数的 FFmpeg 输出不足以裁决边界；本轮用 BS.1770 集成响度复核，差值为 `0.045787 LU`，通过响度匹配门。

### 评分完整性

内部试跑不要求密码学保密，但评分提交前必须生成评分卡与 A/B 音频哈希；揭盲和复核只能追加。正式多评分者实验必须采用访问隔离或追加式存储。

### 代码身份

仅记录 Git HEAD 不足以定位脏工作树。已增加完整 Core Python 文件哈希清单、Git status、tracked patch 及其 SHA-256。该侧车在封口时生成，并由字节一致的重放 WAV连接到原运行结果。后续每次正式运行应在运行开始时自动生成同类来源清单，而不是事后补录。

### Inspector 内存

DeepSeek 在 8 GB 机器上因 525 MB 连续分配失败，属于可扩展性/运行资源 P2，不推翻主处理链重放结果。禁止用手工生成的 Inspector 替代物宣称 Inspector 全链通过。后续实验基础设施应把完整声谱图和批量验证迁移到受控云端 Worker，并记录资源规格。

## 4. 保留边界

- VS-001 的声音技术门仍为失败；
- 人工盲听尚未进行；
- VS-002 至 VS-005 未运行；
- Inspector 全链未在 DeepSeek 的 8 GB 环境重放成功；
- 本次验收不代表专业听感、商业可用或 `PRODUCTION-PROVEN`。

## 5. 封口

第二天工作在“验证集冻结、协议冻结、单样本试跑、失败保留、可复现性和独立审查”范围内完成。当前不进入第三天。后续重要工作应围绕私有实验体系、云端资源标准、不可篡改证据和实验知识继承展开。
