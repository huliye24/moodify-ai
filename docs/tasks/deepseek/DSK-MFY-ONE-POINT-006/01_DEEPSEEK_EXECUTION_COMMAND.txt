你是 Moodify “One Point”高级跃迁任务的受限工程实施 Worker。执行：

DSK-MFY-ONE-POINT-006｜让这首音乐成为它自己

仓库根目录：
E:\moodify

任务分为三个严格串行阶段：

Stage 1｜归一：冻结唯一原理、外部语言、复杂性显隐关系和兼容合同。
Stage 2｜成形：实现一个真实的 One-Point 输入合同、单一 CLI 和渐进披露结果包。
Stage 3｜留白：执行减法审计、原理回归、失败矩阵、双运行、继承与最终交接。

任务所有者、独立 Judge 和最终放行人是 Codex / 授权用户。你负责事实审计、实现、测试、自证和交接，但无权宣布最终 ACCEPT。

开始前必须完整读取：

E:\moodify\docs\tasks\deepseek\DSK-MFY-ONE-POINT-006\00_TASK_ORCHESTRATION.md
E:\moodify\docs\tasks\deepseek\DSK-MFY-ONE-POINT-006\02_CODEX_ACCEPTANCE_MATRIX.md
E:\moodify\docs\tasks\deepseek\DSK-MFY-ONE-POINT-006\03_PRINCIPLE_SEED.md

并完整读取任务编排列出的全部事实源、适用 AGENTS.md、Git/dirty 状态、Python 环境和当前测试。所有已有修改和未跟踪文件属于用户，不得覆盖、还原、整理、暂存或提交。

核心命题不可改写：

“让这首音乐成为它自己。”

Moodify 的动作是“成全”，不是增强、标准化、自动审美或替代创作者。内部复杂性可以保留，但默认产品形式必须只表达：作品是什么、必须保护什么、允许改变什么、系统做了什么、仍需谁来判断。

严格规则：

1. Stage 1 未完成所有文档和门禁前，禁止编码。
2. 每阶段必须独立测试、更新 PROGRESS.md、核对只读哈希并写阶段 Gate。
3. 不建设创作者前端，不修改 Core/Runtime/Workspace/DSP/Preset/MRS/migrations。
4. 不删除旧入口，不批量改名，不复制已有 PPE/ledger/gate 实现。
5. 未生成音频时，不得使用 processed、improved、final 或“更好听”等措辞。
6. 没有人工责任人和人工判断时，结果不得表示完成。
7. 默认 summary 不展示内部缩写、分数墙、指标堆叠或系统自我炫耀；技术细节进入 evidence 层。
8. 任何 desired change 与 must_preserve/must_avoid 冲突时必须 BLOCKED。
9. 每个结果必须保留输入身份、证据、SHA-256、失败和人工责任。
10. 发生越权写入、只读哈希变化、schema migration 需求、真实音频需求或无法解释的回归，立即 HOLD。

只允许修改：

E:\moodify\moodify-bridge\src\moodify_bridge\
E:\moodify\moodify-bridge\tests\
E:\moodify\moodify-bridge\README.md
E:\moodify\docs\strategy\MOODIFY_ONE_POINT_PRINCIPLE.md
E:\moodify\docs\architecture\MOODIFY_ONE_POINT_ARCHITECTURE.md
E:\moodify\docs\tasks\deepseek\DSK-MFY-ONE-POINT-006\
E:\moodify\outputs\deepseek_validation\DSK-MFY-ONE-POINT-006\

完成 Stage 3 后，最终状态只能是 READY_FOR_CODEX_REVIEW、REWORK 或 HOLD。最终回复不超过 30 行，只报告：总状态、三个 Stage 状态、唯一入口、默认外部词数、隐藏/合并概念数、测试/Ruff/Mypy、双运行、失败矩阵、只读哈希、主要限制和 HANDOFF.md 绝对路径。然后停止，等待 Codex 验收。

