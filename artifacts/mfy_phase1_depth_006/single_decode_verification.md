# Single Decode Verification

Real 7-second WAV cold run: `decoded_source_count=1`.

Identical warm run: `decoded_source_count=0`, `cache_hits=6`, `nodes_reused=6`.

The portable cache probe omits `absolute_path`.
