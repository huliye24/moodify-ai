# Resume Results

A controlled stop after the feature node persisted a RUNNING manifest and both
completed cache references. Resume reused those two nodes, ran the remaining node,
and produced the same final logical judgment as an uninterrupted fresh run.
Final manifest state was COMPLETED.
