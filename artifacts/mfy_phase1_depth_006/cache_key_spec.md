# Cache Key Specification

Analysis identity includes source SHA-256, analysis profile, temporal profile,
representation version and measurement registry version.

Node keys additionally include node ID/version, canonical parameters and dependency
logical hashes. Paths and filenames are never authoritative cache identity.
