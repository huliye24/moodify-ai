# MFY-CR-P05 — Unresolved (affects P06+)

1. **PASS_WITH_CAUTION unreachable in v0.1** — IG-03 (reverb) is always
   NOT_MEASURABLE, so any non-PASS dimension escalates to HUMAN_REQUIRED.
   A validated decay/late-energy detector is the unlock for CAUTION semantics.
2. **IG-01 remains PROXY** — no source separation in v0.1; true vocal-identity
   measurement requires stems (P04-adjacent) or a validated mid-band model.
3. **All budgets PROVISIONAL** — need P06 golden candidates + human listening
   records to reclassify CALIBRATED.
4. **Human review not executed** — pairwise protocol is defined; P06 needs the
   first real listening cases (3-5 owned/authorized recordings, manifest only).
5. **Ranking integration** — rank_candidates is standalone; wiring it into the
   data factory algorithmic review (补丁包30) as the identity gate is a P06
   integration decision.
6. **P04 absent** — the package sequence skipped P04 (Reconstruction
   Objective). Candidate generation for golden reconstruction will need to
   build on existing ABC candidates (data_factory) or a P06-internal budget.
