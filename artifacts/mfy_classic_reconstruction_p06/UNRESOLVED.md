# MFY-CR-P06 — Unresolved

1. **Blind listening execution** — kit ready at golden_run_out/listening/;
   human scoring pending. Golden status cannot be claimed before it.
2. **Hardware observation** — two-chain listening pending (consumer +
   reference); protocol ready.
3. **ED-02 noise on this source is unresolved** — floor ~-29 dBFS with no
   quiet windows; the honest INSUFFICIENT_EVIDENCE outcome means noise
   reduction was BYPASSed. A quiet-window-free noise detector is a future
   need (would unlock a second objective axis).
4. **Bandwidth objective is bounded by the estimator** — candidates brighten
   existing content only; true HF restoration above 14 kHz requires validated
   reconstruction of missing spectrum (future, beyond v0.1).
5. **Chain defaults are destructive for era work** — the always-on compressor
   and default reverb forced objective-level bypasses. A chain revision
   (compressor off by default / explicit enable) should be considered by the
   data factory owner.
6. **C is at the edge of the IG-01 proxy budget** (centroid +253 Hz vs 300 Hz
   budget) — worth watching in calibration.
7. **P04** — executed separately per user instruction; this package absorbed a
   minimal objective layer; alignment with the formal P04 deliverable is a
   follow-up.
