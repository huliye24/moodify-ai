# MFY-CR-P06 — Hardware Observations

**Status: PENDING** (requires human listening on real chains; this execution
prepares the protocol and the canonical master stays hardware-neutral).

```text
CHAIN_A (consumer) = pending  (e.g. phone/tablet + common bluetooth/wired buds)
CHAIN_B (reference)= pending  (e.g. better DAC + amp + hi-fi headphones)
```

## Questions to record per chain

```text
difference_noticeability =
preference =
identity_safety =
vocal_change =
low_end_change =
space_change =
fatigue =
confidence =
```

## Cross-chain questions

```text
Does the same winner remain preferred?
Does the reconstruction benefit survive on consumer hardware?
Does reference hardware expose artifacts hidden on consumer devices?
Does consumer hardware hide improvements visible on reference devices?
```

## Boundary (hard rule)

No hardware observation may modify the canonical reconstruction master.
Device adaptation is downstream (Listening Environment), never permanent.
`future_device_rendering_need` is recorded only.
