# Network Policy

No Windows metered-network capability adapter exists, and W10 has no active polling/download path. Wi-Fi-only, metered, background refresh and download preferences are therefore hidden. Existing BFF request timeout/retry remains an API-client concern, not a user-facing engineering knob.
