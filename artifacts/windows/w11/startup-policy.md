# Startup Policy

`launch_at_startup` is fixed false and hidden. Although Electron exposes a login-item API, packaged executable identity, installer registration, upgrade and uninstall cleanup require W12 evidence. W11 does not offer a switch that might lie or leave stale registration.

Future startup registration must default OFF and must never turn W08 restore into autoplay.
