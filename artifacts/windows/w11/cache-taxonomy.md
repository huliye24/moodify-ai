# Cache Taxonomy

Current desktop build has no owned thumbnail, cloud temporary, prepared-source or download cache subsystem. Therefore `CACHE = NOT_IMPLEMENTED` and no fake size/clear control appears.

Durable data is never cache: Library/Track membership, Playlist, Favorite, History, Recovery/Queue state, Settings and original audio. W12 may classify packaging/build artifacts separately; they are not runtime user cache.
