# Uninstall / Reinstall Test

Policy is preserve-user-data and never delete original audio. No installer lifecycle run was executed, so binary removal, integration cleanup and post-reinstall data restoration are not verified. `UNINSTALL = NOT_VERIFIED`; `REINSTALL = NOT_VERIFIED` (P1).
