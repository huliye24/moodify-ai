# Clean Machine Test

Production packaging proves no dev-server dependency in the artifact. A clean VM/machine install → launch → import → play → restart → uninstall run was not executed. `CLEAN_INSTALL = NOT_VERIFIED` (P1).
