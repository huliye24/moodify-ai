# Versioning Policy

`package.json` is the single product-version authority: `0.1.0-beta.1`. Forge reads it directly. Windows numeric build is `0.1.0.5`; channel is `BETA_CANDIDATE`; commit is `UNKNOWN` because the implementation directory has no Git metadata. User-visible identity is `Moodify 0.1.0-beta.1`.
